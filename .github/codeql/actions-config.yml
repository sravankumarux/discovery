# CodeQL configuration for workflow expressions and embedded scripts.
# security-and-quality adds workflow correctness checks to the extended
# security analysis and is maintained by GitHub for the Actions language.

name: "Discovery GitHub Actions"

paths:
  - .github/workflows

queries:
  - uses: security-and-quality